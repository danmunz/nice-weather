# isitniceout.com

A minimalist weather app. One question, one answer.

## Development

Open `index.html` in a browser. No build step required.

## Deployment

Pushes to `main` auto-deploy to GitHub Pages via the workflow in `.github/workflows/deploy.yml`.
